name: Shell Commend
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world"
      - run: |
          node -v
          npm -v
          touch index.css
      - name: python Commend
        run: |
          import platform
          print(platform.processor())
        shell: python
